package stolr.graphstream.test;

import java.util.Iterator;
import java.util.Scanner;

import org.graphstream.graph.Edge;
import org.graphstream.graph.Graph;
import org.graphstream.graph.Node;


  class PropagationThread extends Thread {
     
	 

         public void run(Graph graphF , UserFacebook f) {
        	 
        	 		try {
						Thread.sleep(500);
					} catch (InterruptedException e) {
						
        			
						
        		
        			int credibR = sc.nextInt();
        			Rumeur r = new Rumeur(nomR, credibR);

        			
        			//PropagerRumeur(r, graphF.getNode(f.getId()),graphF.getNode(f.getId()),graphF);

         }
         
     }